<div class="container-fluid">
  <div class="row mt-5">
    <div class="col-12 col-lg-3">
      <%= render 'public/customers/side_bar', customer: @customer %>
    </div>

    <div class="col-12 col-lg-9">
      <%= render 'public/customers/list', customers: @customers %>
      <div class="mx-auto">
        <div class="pagination">
          <%= paginate @customers %>
        </div>
      </div>
    </div>

  </div>
</div>