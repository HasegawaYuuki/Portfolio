<% if review.favorited_by?(current_customer) %>
  <%= link_to review_favorites_path(review), method: :delete, remote: true, data: {"turbolinks" => false} do %>
    <div class="favolit-btn">
      <%= review.favorites.count %>  <i class="fa-solid fa-bookmark" aria-hidden="true" style="color: black;"></i>
    </div>
  <% end %>
<% else %>
  <%= link_to review_favorites_path(review), method: :post, remote: true, data: {"turbolinks" => false} do %>
    <div class="favolit-btn">
      <%= review.favorites.count %>  <i class="fa-regular fa-bookmark" aria-hidden="true"></i>
    </div>
  <% end %>
<% end %>